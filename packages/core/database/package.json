{
  "name": "@lanceflow/database",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "prisma generate",
    "typecheck": "prisma generate && tsc --noEmit",
    "lint": "prisma generate && tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "node scripts/run-prisma.mjs migrate dev",
    "db:migrate:deploy": "node scripts/run-prisma.mjs migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "@lanceflow/typescript-config": "workspace:*",
    "@types/node": "^26.1.1",
    "prisma": "^6.1.0",
    "typescript": "^7.0.2"
  }
}
